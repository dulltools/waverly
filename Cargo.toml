[package]
name = "waverly"
version = "0.1.0"
edition = "2021"
description = "Parse and write WAV files"
license = "MIT"
authors = ["aef-"]
repository = "https://github.com/fraihaav/waverly/"
exclude = ["meta/*"]

[dependencies]
binrw = { version = "0.8.4" }

